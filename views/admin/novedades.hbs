<div class="container">
    {{!-- fila1 --}}
    <div class="row">
        <div class="col">
            <p class="text-end">
                <span>Hola {{usuario}}!!</span>
                <a href="/admin/login/logout" class="btn btn-danger"><i class="fas fa-sing-out-alt">cerrar sesion</i></a>
            </p>
        </div>
    </div>
 {{!-- fin fila1 --}}

  {{!-- fila2 --}}
  <div class="row">
      <div class="col-7" style="margin-top: 9px;">
          <h2>listado de novedades</h2>
      </div>
      <div class="col-5 text-end">
          <a href="/admin/novedades/agregar" class="btn btn-success"><i class="fas fa-plus"> Nuevo</i></a>
      </div>
  </div>
  {{!-- fin fila1 --}}

  {{!-- fila 3 --}}
    <div class="row">
        <div class="col">
            <table class="table">
                <thead>
                    <tr class="table-dark">
                        <th scope="col">#</th>
                        <th scope="col">titulo</th>
                        <th scope="col">Imagen</th>
                        <th scope="col" class="text-end">acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each novedades}}
                    <tr>
                        <th scope="row">{{id}}</th>
                        <td>{{titulo}}</td>
                        <td>{{{imagen}}}</td>
                            <td class="text-end">
                                <a href="/admin/novedades/modificar/{{id}}" class="btn btn-secondary"><i
                                class="fas fa-pencil-alt"></i></a>
                                <a href="/admin/novedades/eliminar/{{id}}" class="btn btn-secondary"><i class="fas fa-trash"></i></a>                           
                            </td>
                    </tr>


                        <!-- Modal -->
                 <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title" id="exampleModalLabel">ATENCION</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    ¿Deseas borrar este contenido? una vez eliminado no se podra volver a recuperar.
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Cancelar</button>
                                    <a href="/admin/novedades/eliminar/{{id}}" class="btn btn-primary">Borrar definitivamente</a>
                                </div>
                                </div>
                        </div> 
                  </div>
                    {{/each}}
                </tbody>
            </table>
        </div>
        </div>

  {{!-- fin fila 3 --}}
</div>

{{!-- <a href="/admin/novedades/eliminar/{{id}}" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
                                   <i class="fas fa-trash"></i>
                                </a> --}}